import { createStore } from "vuex";

let state = {
    userId: 'test_user'
}
let mutations = {
    
}
let getters = {
    
}
let actions = {
    
}

export const store = createStore({
    state: state,
    mutations: mutations,
    getters: getters,
    actions: actions
})